<?php
ini_set('display_errors', 1);
if (! defined('BASE_PATH')) define('BASE_PATH', __DIR__);
if (! defined('SONATA_PATH')) define('SONATA_PATH', realpath(__DIR__ ));

require_once SONATA_PATH . '/Sonata/autoload.php';

